{
  "name": "ai-startup",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "build": "cd frontend && npm run build"
  }
}
